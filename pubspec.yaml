name: zarity_health_assignment_vedant_shrivastava
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  app_links: ^6.4.0
  cached_network_image: ^3.4.1
  cloud_firestore: ^5.6.4
  firebase_core: ^3.12.0
  firebase_storage: ^12.4.3
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.0
  image_picker: ^1.1.2
  share_plus: ^10.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
